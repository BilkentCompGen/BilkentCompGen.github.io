source 'https://rubygems.org'

gem "jekyll", "~> 4.4"
gem "jekyll-sitemap"

# Gems below are only needed by features that are currently switched off.
# Uncomment together with the code that uses them.
#
# gem "execjs"                 # _plugins/eq.rb, eqinline.rb  -> KaTeX math tags
# gem "octokit"                # _scripts/generate-project-data.rb
# gem "netrc"                  # _scripts/generate-project-data.rb
# gem "jekyll-twitter-plugin"  # _plugins/tweet.rb
